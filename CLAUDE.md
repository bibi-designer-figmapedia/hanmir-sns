# 한미르 인스타 콘텐츠 파이프라인 (2-에이전트)

브랜드 인스타그램 운영을 위한 2단계 서브 에이전트 파이프라인. 브랜드는 `BRAND.md`가 정한다 (실습 예시: 분독 @boondog_official).
경쟁사를 긁어 보고서를 만들고, 그걸 근거로 우리 콘텐츠를 기획해 피그마 템플릿에 채운다.

## 파이프라인

```
BRAND.md  (브랜드 설명서 — 브랜드 컬러·제품·말투·제약·무드·경쟁사 [+피그마 URL·템플릿 스타일 가이드])   ← 수강생/클라이언트가 자기 브랜드로 쓴다
   └ 수업 초반: "피그마 템플릿 학습해줘 {피그마 URL}" → 부모가 템플릿 구조(배리언트·레이어)와 스타일 가이드를 읽어 7번을 채운다

ig-competitor-scout   Apify 수집 → research/{핸들}/{YYYY-MM}/ (posts.json + jpg)
                      → outputs/research/{날짜}_competitor-report.{json,md,pptx}
        ↓ [게이트 ①] 사람이 pptx 확인
ig-content-maker      입력: 브리프(제품 · 희망 주제/무드 · 건수) + BRAND.md (+ 보고서 있으면 참고)
                      A. 회차별 구성 → outputs/planning/{YYYY-MM}_content-plan.md (회차 = 캐러셀 3~5장: Cover → Body… → Ending, 장마다 카피 + photo(light/dark) + 이미지 프롬프트)
                      B. 틀 → "피그마로 뽑아줘 {URL}" → 템플릿을 읽어 장 역할(Cover/Body/Ending)에 맞는 배리언트에 카피 채움 — 회차당 한 줄 (📥 Output)
                         템플릿 액센트 컬러 ≠ BRAND.md 브랜드 컬러면 버튼·강조 줄을 브랜드 컬러로 치환
        ↓ [게이트 ②] 사람이 피그마에서 확인·수정
        ↓ 사람이 Make an image로 이미지 채움 (기획서의 프롬프트)
```

## 위임 규칙

에이전트 파일은 `.claude/agents/`의 둘: `ig-content-maker.md`(완성본)와 `ig-competitor-scout.md`(완성본 — 수업에서는 수강생이 빈칸 카드로 직접 만든다. 파일이 없거나 카드가 비어 있으면 scout를 부르기 전에 "에이전트 파일부터 만드세요"라고 안내한다).

| 요청 유형 | 위임 대상 |
|---|---|
| 경쟁사 수집·분석·보고서·레퍼런스 | ig-competitor-scout |
| 콘텐츠 기획·회차 구성·게시물 틀·피그마로 뽑기 | ig-content-maker |

**직접 하지 않는다.** 두 작업 중 하나에 해당하면 반드시 위임한다. 부모가 직접 처리하면
컨텍스트가 오염되고 산출물이 파일로 남지 않는다.

**한 번에 하나씩.** "경쟁사 분석부터 피그마까지 다 해줘"를 받아도 둘을 연달아 자동 실행하지 않는다.
scout를 돌리고, 게이트 ①을 사람에게 확인받고, 그다음 maker로 간다.
`outputs/research/`에 **오늘 날짜 보고서가 이미 있으면 scout를 다시 돌리지 않는다** — 그 경로를 보여주고 게이트 ①부터 시작한다(다시 수집하려면 사용자가 "다시 수집해줘"라고 해야 한다).

**maker는 scout 없이도 돈다.** 브리프(제품·주제·건수)와 BRAND.md만 있으면 회차 구성이 된다. 보고서는 있으면 참고.

**브리프 인터뷰는 부모가 한다.** 서브 에이전트는 사용자와 대화할 수 없다(질문하면 거기서 끝난다).
"콘텐츠 기획해줘"처럼 브리프 없이 요청이 오면 maker를 부르기 전에 **한 번의 메시지로** 세 가지를 묻는다:

1. 제품 — 비우면 `BRAND.md 2. 제품`의 "이번 달 밀 제품"
2. 희망 주제 또는 무드 — **필수** (예: 가을 산책 루틴 / 신상 컬러 소개 / 따뜻하고 조용한)
3. 건수(게시물 수 — 게시물 하나가 캐러셀 3~5장) — 비우면 3

답을 받으면 `제품 / 주제·무드 / 건수` 한 줄로 정리해 maker에 넘긴다. 사용자가 처음부터 브리프를 말했으면 묻지 않고 바로 넘긴다.

## 게이트 (자동 통과 금지)

서브 에이전트는 최종 요약만 돌려준다. 중간에 무슨 일이 있었는지 부모는 못 본다. 그래서 게이트가 필수다.

**게이트 ①** — scout 완료 후. pptx 경로와 함께 사용자에게 제시한다:
- 미확보 데이터(이미지 만료, 비공개 좋아요, 수집 실패 계정)
- 근거 URL이 2개 미만인 패턴이 없는지
사용자가 "다음"이라고 하기 전에 maker를 호출하지 않는다.

**게이트 ②** — maker 완료 후. 피그마 링크와 함께 확인받는다.

*기계가 볼 수 있는 것은 maker가 먼저 확인하고 **결과만** 보고한다* (사람에게 다시 묻지 않는다):
글자 잘림 · 이미지 프롬프트에 구역 지시("위쪽은 비워라", "kept plain", "empty space" 같은 말)가 섞였는지 · 프롬프트가 한 문장인지 · 브랜드 컬러 치환 수(`swapped`)와 흰 글자 전환 수(`whitened`).

*사람이 봐야 하는 것만 묻는다:*
- 이미지 프롬프트가 BRAND.md 4(장소·모델·소재)·5(무드) 안인가 — Make an image로 만들어도 브랜드가 실제로 보여줄 수 있는 장면이어야 한다
- 캐러셀 흐름이 읽히는가 — Cover 후킹 → Body 한 장에 한 메시지 → Ending 제품·CTA. 썸네일 한 장짜리·훅 문구 베리에이션이면 반려
- 카피가 BRAND.md의 언어·말투인가 (짧게)
- 사진 위에 글자가 얹히는 장에서 `photo` 판정(dark/light)이 그 프롬프트의 사진과 맞는가 — dark면 흰 글자로 바뀌어 있다

## 배운 것은 규칙으로 올린다 (사람이 매번 고치지 않게)

게이트에서 사람이 지적한 것을 **그 게시물만 고치고 끝내지 않는다.** 고친 뒤 "같은 일이 다시 안 생기려면 어디에 적어야 하는가"를 판단해 **한 줄로 제안하고 승인받아 적는다.** 어디에 적는지는 "무엇에만 참인가"로 정한다:

| 지적한 내용이… | 적는 곳 |
|---|---|
| 이 브랜드에서만 참 (제품·말투·나오면 안 되는 것) | `BRAND.md` 해당 섹션 |
| 이 템플릿에서만 참 (레이어·색·구조) | `BRAND.md 7번` |
| 어느 브랜드에서나 참인 **콘텐츠 판단** (카피 길이, 장 구성, 프롬프트 쓰는 법) | 그 에이전트 파일 |
| 어느 브랜드에서나 참인 **순서·검문·도구 한계** | `CLAUDE.md`(알려진 함정 / 게이트) |
| 피그마 조작 방법 자체 | `scripts/figma-*.js` |

기계가 매번 검사할 수 있는 지적이면 게이트 항목으로 두지 말고 **스크립트나 에이전트 자가점검으로 내린다** — 게이트에는 사람 눈으로만 되는 것만 남긴다.
사용자는 "왜 이렇게 나왔어?"라고만 말하면 된다. 원인을 찾고, 위 표로 적을 곳을 정하고, **적을 문장을 보여주고 승인받는 것**까지가 한 번의 답이다.

## 파일로 체이닝한다

에이전트 간 전달은 **파일 경로**로 한다. 보고서 전문을 다음 에이전트 프롬프트에 붙이지 않는다.
maker를 호출할 때는 브리프와 함께 `outputs/research/{날짜}_competitor-report.md` **경로만** 준다(있을 때). maker가 직접 읽는다.

## 컨텍스트 파일

- `BRAND.md` — 브랜드 설명서 7섹션 (1 한 줄(+브랜드 컬러) · 2 제품★ · 3 말투와 언어 · 4 이미지에 나올 것★ · 5 이미지 무드 · 6 경쟁사★ · 7 피그마 URL + 템플릿 스타일 가이드(선택)). **어떤 브랜드든 이 칸들이면 파이프라인이 돈다.** ★만 필수, 나머지는 에이전트 기본값. 피그마 URL은 "피그마로 뽑아줘 {URL}"처럼 프롬프트에 줘도 되고 7번에 적어둬도 된다. 템플릿 구조(배리언트·레이어·글자색·액센트 컬러)는 maker가 피그마에서 직접 읽는다 — 표로 적지 않는다.
- **피그마 템플릿 학습 (BRAND.md 7번)** — 사용자가 "피그마 템플릿 학습해줘 {피그마 URL}"(또는 "콘텐츠 템플릿 읽어서 BRAND.md에 정리해줘 {URL}")라고 하면 **부모가 직접**(위임 없이) 7번을 채운다. 링크 하나로 끝나야 한다 — 사용자에게 추가로 묻지 않는다:
  1. `- 파일 URL:` 에 그 URL을 적는다 (node-id는 떼고 파일 URL만).
  2. Figma MCP `get_metadata`로 페이지를 훑어 **`txt-` 텍스트 레이어가 들어 있는 컴포넌트 세트**를 찾는다(이미지 슬롯 세트 `image`는 템플릿이 아니다). 7번에 **템플릿 구조 한 줄**을 적는다: `템플릿 구조: 배리언트 cover · body-1 · body-2 · ending / 텍스트 레이어 txt-headline · txt-body · txt-cta · txt-option(-2) · txt-caption(-2) · txt-brand / 이미지 슬롯 image`. `txt-` 레이어가 하나도 없으면 "텍스트 레이어 이름을 txt-로 시작하게 바꿔주세요"라고 안내하고 멈춘다.
  3. 이름이 `Style Guide`로 시작하는 프레임(`Style Guide — Color` · `Style Guide — Typography`)이 있으면 텍스트 레이어 이름에서 값을 읽어 **템플릿 스타일 가이드 표**를 채운다: 액센트 컬러(가장 진한 채도 있는 색) / 기본 글자색 / 폰트 / 타입 스케일. 보조 컬러(Sage·Cream 같은 연한 색)가 있으면 `보조 컬러` 행을 추가하고, 템플릿 컴포넌트에서 안 쓰이면 `(템플릿 미사용 — 가이드에만 있음)`. 프레임이 없으면 표는 `{…}`로 두고 "피그마에서 템플릿을 선택하고 Figma 에이전트에 `지금 템플릿에서 사용된 컬러 그리고 타이포그래피 스타일 가이드로 만들어줘`라고 한 뒤 다시 불러주세요"라고 안내한다 — 구조 한 줄은 이미 채웠으니 파이프라인은 돈다.
  4. **템플릿 점검 한 줄.** `scripts/figma-read-template.js`를 돌려 `warn`이 있는 배리언트를 그대로 알려준다 — 글자가 사진 위에 그대로 얹힌 배리언트는 사진 밝기에 따라 글자가 묻힌다. 두 가지 방법을 알려준다: (1) 기획서 슬라이드마다 `photo`(light/dark)를 정하면 maker가 어두운 사진 위 글자를 흰색으로 바꾼다(기본 동작), (2) 아예 안 흔들리게 하려면 그 배리언트에 반투명 스크림을 깔거나 글자를 사진 밖으로 옮긴다 — **템플릿에서 한 번** 고치면 끝난다.
  5. 채운 내용을 보여주고 한 줄: "1번 브랜드 컬러가 `{액센트 hex}`와 다르면 게시물을 만들 때 버튼·강조 줄이 브랜드 컬러로 바뀝니다. 지금 1번은 `{값 또는 비어 있음}`."
  **CLAUDE.md는 건드리지 않는다.** 템플릿이 바뀌어도 절차는 그대로다 — 바뀌는 값은 BRAND.md 7번에만 적는다(CLAUDE.md까지 고치기 시작하면 사람마다 절차가 달라져 같은 저장소가 아니게 된다). BRAND.md의 다른 섹션도 건드리지 않는다. 이게 "BRAND.md는 사람이 관리한다"의 유일한 예외다. 브랜드 컬러(1번)는 사람이 적는다 — 템플릿 색을 거기 옮겨 적지 않는다. `scripts/figma-*.js`는 만들지 않는다 — 저장소에 이미 있다.
- **BRAND.md 초안을 Apify로 채우기** — 사용자가 "apify로 이 링크의 계정을 분석하고 BRAND.md 채워줘 {인스타 URL}"라고 하면 **부모가 직접** 한다: Apify `instagram-scraper`로 그 계정 최근 게시물 20건을 **1회만** 수집 → 프로필 소개·캡션·해시태그에서 1번(브랜드명·한 문장·계정), 3번(카피 언어·자주 쓰는 표현·태그), 5번(분위기·주로 보이는 것)을 채운다. 2번 제품·4번 이미지에 나올 것·6번 경쟁사는 게시물만으로는 모르니 `{…}`로 두고 "이 세 칸은 직접 채워주세요"라고 한다. 지어내지 않는다 — 캡션에 없는 제품명·가격을 쓰지 않는다.
- `@import`하지 않는다. 각 서브 에이전트가 자기 컨텍스트에서 직접 읽는다. 부모는 경로만 알면 된다.
- BRAND.md의 ★ 섹션이 `{…}`면 에이전트를 부르기 전에 사용자에게 채워달라고 한다. ★ 아닌 섹션은 비어 있어도 된다.

## 알려진 함정

**인스타 크롤링 스크립트를 짜지 말 것.** 로그인 월 + 약관으로 막힌다. 수집은 Apify MCP 한 경로만.

**Apify `displayUrl`은 수 시간 내 만료된다.** 계정 하나 수집 직후 `scripts/fetch-images.js`를 바로 돌린다. 나중에 몰아서 하면 이미지가 사라진다.

**좋아요 `-1`은 "비공개"다.** 0이나 결측으로 취급하지 않고, 중앙값 계산에서 제외한다.

**중앙값으로 말한다.** 평균은 아웃라이어 하나에 망가진다.

**Figma MCP 한도: 분당 10회 · 일 200회.** 게시물 1건(캐러셀 3~5장) = `use_figma` 1회. 장 하나씩 부르지 않는다.

**이미지는 에이전트가 만들지 않는다.** Figma의 Make an image는 플러그인 API에 없어서 MCP로 못 부른다. 에이전트는 프롬프트까지만, 생성은 사람이 피그마에서.

**`figma.currentPage = page`는 안 된다.** `await figma.setCurrentPageAsync(page)`만 동작하고, 호출당 1회만.

**이미지 프롬프트에 글자를 넣지 않는다.** 생성 모델의 글자는 깨진다. 글자는 템플릿 텍스트 레이어가 얹는다.

**텍스트 수정 전 폰트 로드.** `loadFontAsync` 없이 `characters`나 `setProperties`를 치면 실패한다.

**텍스트는 레이어 이름으로 채운다** (`txt-headline/body/cta/option/caption` + 자동 `txt-brand`, 같은 칸이 여럿이면 `-2`). 컴포넌트 프로퍼티는 쓰지 않는다. 피그마 스크립트는 `scripts/figma-*.js` — 에이전트 파일에 코드를 적지 않는다.

## 산출물 규칙

- 수집 원문: `research/{핸들}/{YYYY-MM}/posts.json` + `{shortCode}.jpg` (이미지는 git 제외)
- 보고서: `outputs/research/{YYYY-MM-DD}_competitor-report.{json,md,pptx}`
- 기획: `outputs/planning/{YYYY-MM}_content-plan.md` (회차 = 캐러셀 3~5장, 장마다 이미지 프롬프트·피그마 노드 링크 포함)
- 피그마: 템플릿 파일의 `📥 Output` 페이지에만 생성(없으면 maker가 만든다). 템플릿 컴포넌트 원본(`Contents Template` 섹션) 수정 금지
- `BRAND.md`는 사람이 관리한다. 서브 에이전트는 수정하지 않는다 (예외: 부모가 7번만 채움 — 피그마 템플릿 학습)

## 콘텐츠 원칙

기획 검토 시 아래에 어긋나면 지적한다.

- **기획서에는 우리 기획만.** 경쟁사 URL·계정명·"차용 레퍼런스"·근거 설명을 기획서에 쓰지 않는다 — 기획서는 클라이언트에게 그대로 나가는 문서라서. 경쟁사 근거는 scout 보고서(pptx·md)에만 남기고, maker는 그걸 머릿속 참고로만 쓴다.
- **제품 노출 방식은 BRAND.md 5. 이미지 무드를 따른다.** 따로 없으면 제품이 실제 쓰이는 장면 안에 있게.
- **게시물은 캐러셀 3~5장.** Cover(후킹) → Body(한 장에 한 메시지) → Ending(제품·CTA). 썸네일 한 장에 훅 문구만 바꾼 베리에이션은 콘텐츠가 아니다.
- **목표는 하나만.** 도달과 커뮤니티는 다른 콘텐츠가 만든다.
- **안전·법적 리스크 연출 금지.** 위험 상황 재현, 의약품처럼 들리는 효능 단정 등 업종별 금지 표현은 BRAND.md 3의 "안 하는 것"을 따른다.
- **검증 불가 효능·최상급 표현 금지.**

## 부르는 말 (슬래시 커맨드 없음 — 자연어로)

- "경쟁사 분석해줘" / "레퍼런스 수집해줘" (+ 핸들·기간 지정 가능) → scout
- "콘텐츠 기획해줘" / "{제품} / {주제} / {건수}로 구성해줘" → maker A · "피그마로 뽑아줘 {URL}" → maker B
- "피그마 템플릿 학습해줘 {URL}" → 부모가 직접 (7번: URL · 템플릿 구조 · 스타일 가이드 표)
- "apify로 이 링크의 계정을 분석하고 BRAND.md 채워줘 {인스타 URL}" → 부모가 직접 (1·3·5번 초안)
